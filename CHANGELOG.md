# Changelog

Notable changes to `kleros-disputant-cli`. Format loosely follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versions follow semver.

## [Unreleased]

Nothing published yet. The package is unreleased on npm on purpose: a published version
makes any change to how evidence payloads or dispute templates are constructed breaking
for installs beyond one machine. See `docs/adr/`.
